select distinct object from yagofacts where to_tsvector('simple', regexp_replace(subject, E'[^A-Za-z0-9]', ' ', 'g')) @@ to_tsquery('Albert_Einstein') and predicate='<isMarriedTo>';
select distinct object from yagofacts where to_tsvector('simple', regexp_replace(subject, E'[^A-Za-z0-9]', ' ', 'g')) @@ to_tsquery('Barack_Obama') and predicate='<isMarriedTo>';
select distinct object from yagofacts where to_tsvector('simple', regexp_replace(subject, E'[^A-Za-z0-9]', ' ', 'g')) @@ to_tsquery('Ella_Fitzgerald') and predicate='<hasMusicalRole>';
select distinct object from yagofacts where to_tsvector('simple', regexp_replace(subject, E'[^A-Za-z0-9]', ' ', 'g')) @@ to_tsquery('Goldman_Sachs') and predicate='<owns>';
select distinct object from yagofacts where to_tsvector('simple', regexp_replace(subject, E'[^A-Za-z0-9]', ' ', 'g')) @@ to_tsquery('Samoa') and predicate='<hasCapital>';
select distinct object from yagofacts where to_tsvector('simple', regexp_replace(subject, E'[^A-Za-z0-9]', ' ', 'g')) @@ to_tsquery('Irene_Ware') and predicate='<actedIn>';
select distinct object from yagofacts where to_tsvector('simple', regexp_replace(subject, E'[^A-Za-z0-9]', ' ', 'g')) @@ to_tsquery('Alaska') and predicate='<owns>';
select distinct object from yagofacts where to_tsvector('simple', regexp_replace(subject, E'[^A-Za-z0-9]', ' ', 'g')) @@ to_tsquery('Bryan_College') and predicate='<isLeaderOf>';
select distinct object from yagofacts where to_tsvector('simple', regexp_replace(subject, E'[^A-Za-z0-9]', ' ', 'g')) @@ to_tsquery('Bryan_College') and predicate='<hasMotto>';
select distinct object from yagofacts where to_tsvector('simple', regexp_replace(subject, E'[^A-Za-z0-9]', ' ', 'g')) @@ to_tsquery('John_Selden') and predicate='<isInterestedIn>';
select distinct object from yagofacts where to_tsvector('simple', regexp_replace(subject, E'[^A-Za-z0-9]', ' ', 'g')) @@ to_tsquery('Helen_Miller') and predicate='<isLeaderOf>';
select distinct object from yagofacts where to_tsvector('simple', regexp_replace(subject, E'[^A-Za-z0-9]', ' ', 'g')) @@ to_tsquery('Adam_Chandler') and predicate='<hasChild>';
select distinct object from yagofacts where to_tsvector('simple', regexp_replace(subject, E'[^A-Za-z0-9]', ' ', 'g')) @@ to_tsquery('Katie_Wright') and predicate='<actedIn>';
select distinct object from yagofacts where to_tsvector('simple', regexp_replace(subject, E'[^A-Za-z0-9]', ' ', 'g')) @@ to_tsquery('Nigeria') and predicate='<hasCapital>';
select distinct object from yagofacts where to_tsvector('simple', regexp_replace(subject, E'[^A-Za-z0-9]', ' ', 'g')) @@ to_tsquery('Vietnam') and predicate='<hasCapital>';
select distinct object from yagofacts where to_tsvector('simple', regexp_replace(subject, E'[^A-Za-z0-9]', ' ', 'g')) @@ to_tsquery('George_Shaw') and predicate='<wroteMusicFor>';
select distinct object from yagofacts where to_tsvector('simple', regexp_replace(subject, E'[^A-Za-z0-9]', ' ', 'g')) @@ to_tsquery('Gilberto_Gil') and predicate='<wroteMusicFor>';
select distinct object from yagofacts where to_tsvector('simple', regexp_replace(subject, E'[^A-Za-z0-9]', ' ', 'g')) @@ to_tsquery('Gord_Perks') and predicate='<isLeaderOf>';
select distinct object from yagofacts where to_tsvector('simple', regexp_replace(subject, E'[^A-Za-z0-9]', ' ', 'g')) @@ to_tsquery('David_Hume') and predicate='<isInterestedIn>';
select distinct object from yagofacts where to_tsvector('simple', regexp_replace(subject, E'[^A-Za-z0-9]', ' ', 'g')) @@ to_tsquery('Grenada') and predicate='<hasCapital>';
